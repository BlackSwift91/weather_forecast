import React from 'react';

import { HeaderButton, HeaderButtonProps } from 'react-navigation-header-buttons';

export const CustomHeaderButton = (props: HeaderButtonProps) => <HeaderButton iconSize={23} {...props} />;
